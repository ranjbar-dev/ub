// Package mocks provides mock implementations of service interfaces for
// use in integration tests (test/ directory). Mocks are created using
// testify/mock and implement the same interfaces as production services.
package mocks
