import 'package:flutter/material.dart';
import 'UBLoading.dart';

class CenterUbLoading extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: UBLoading(),
    );
  }
}
