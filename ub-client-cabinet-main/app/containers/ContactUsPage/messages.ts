/*
 * ContactUsPage Messages
 *
 * This contains all the text for the ContactUsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'containers.ContactUsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ContactUsPage container!',
  },

  king: {
    id: `${scope}.king`,
    defaultMessage: 'default king',
  },
});
