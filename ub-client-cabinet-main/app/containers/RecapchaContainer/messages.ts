/*
 * RecapchaContainer Messages
 *
 * This contains all the text for the RecapchaContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'containers.RecapchaContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RecapchaContainer container!',
  },
});
